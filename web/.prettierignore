pnpm-lock.yaml
dist
node_modules

# Vendored TS contract — generated by scripts/sync-contract.sh from ts-rs
# (engine repo). It is committed in ts-rs' own compact format; prettier must not
# reformat it, or `just fix` / format would diverge from what the drift check
# (scripts/check-contract-drift.sh) regenerates and silently break the gate.
src/contract/
